# CustomerWebApplication
It is the UI application of the Customer Web Application. It performs the CRUD operations of the customer. 
It was developed with ASP.NET Core (Razor Pages). 
It uses the CustomerWebAPI project as a Web API for data access. After the Web API is run, you must assign the URL created to the **WebAPI** variable defined in the **appsettings.json** file.
